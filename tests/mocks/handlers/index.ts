import type { RequestHandler } from 'msw'

// Combine all module handlers here as they are created
export const handlers: RequestHandler[] = [
  // TODO(F1-01): Add auth handlers
  // TODO(F4-01): Add recruitment handlers
]
